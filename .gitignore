# Managed by squad-kit — do not edit this block
.squad/secrets.yaml
.squad/stories/**/attachments/
.squad/.trash/
.squad/.last-run.json
.squad/.last-copy-prompt.md
.squad/runs/
# End squad-kit block

# --- Story 02: monorepo scaffolding ---

# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
out/

# Turborepo
.turbo/

# TypeScript
*.tsbuildinfo

# Prisma
apps/api/prisma/migrations/dev.db
apps/api/generated/

# Environment
# Deliberately broad: `.env.*` catches the real deployment env files
# (`.env.production`, `.env.staging`, ...) that docker-compose.prod.yml reads
# via `--env-file`. Only the committed *templates* are re-admitted below.
.env
.env.*
!.env.example
!.env.production.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor/OS
.DS_Store
Thumbs.db

# Testing
coverage/
playwright-report/
test-results/

# Docker volumes (bind-mounted local data, if any land in-repo)
.data/

