# Plans index

One row per feature folder under `.squad/plans/`. `NN` continues as a global execution sequence across all features when `naming.globalSequence` is `true` in `config.yaml`.

| Feature              | Overview                                                                     | NN range |
| -------------------- | ------------------------------------------------------------------------------- | -------- |
| project-foundation   | [project-foundation/00-overview.md](./project-foundation/00-overview.md)       | 01–05    |
| customer-management  | [customer-management/00-overview.md](./customer-management/00-overview.md)     | 06       |
| ticketing            | [ticketing/00-overview.md](./ticketing/00-overview.md)                         | 07–09    |
| sla-policy-foundation | [sla-policy-foundation/00-overview.md](./sla-policy-foundation/00-overview.md) | 10–      |
| background-job-producer-foundation | [background-job-producer-foundation/00-overview.md](./background-job-producer-foundation/00-overview.md) | 14–      |
| sla-timer-detection-foundation | [sla-timer-detection-foundation/00-overview.md](./sla-timer-detection-foundation/00-overview.md) | 15–      |
| ticket-recategorization-sla-target-recomputation | [ticket-recategorization-sla-target-recomputation/00-overview.md](./ticket-recategorization-sla-target-recomputation/00-overview.md) | 16–      |
| sla-breach-escalation | [sla-breach-escalation/00-overview.md](./sla-breach-escalation/00-overview.md) | 17–      |
| sla-at-risk-notification-reaction | [sla-at-risk-notification-reaction/00-overview.md](./sla-at-risk-notification-reaction/00-overview.md) | 18–      |
| ticket-escalation-notification-reaction | [ticket-escalation-notification-reaction/00-overview.md](./ticket-escalation-notification-reaction/00-overview.md) | 19–      |
| realtime-socketio-foundation | [realtime-socketio-foundation/00-overview.md](./realtime-socketio-foundation/00-overview.md) | 20–      |
| ticket-history-timeline-completion | [ticket-history-timeline-completion/00-overview.md](./ticket-history-timeline-completion/00-overview.md) | 21–      |
| in-app-notification-delivery | [in-app-notification-delivery/00-overview.md](./in-app-notification-delivery/00-overview.md) | 22–      |
| agent-workspace-ticket-operations-mvp | [agent-workspace-ticket-operations-mvp/00-overview.md](./agent-workspace-ticket-operations-mvp/00-overview.md) | 23–      |
| _(unplanned)_ agent-workspace-notification-display | Story 24 — implemented directly from a user-supplied specification; no `.squad` representation exists for it | 24        |
| agent-workspace-ticket-customer-creation | [agent-workspace-ticket-customer-creation/00-overview.md](./agent-workspace-ticket-customer-creation/00-overview.md) | 25–      |
| agent-workspace-customer-management | [agent-workspace-customer-management/00-overview.md](./agent-workspace-customer-management/00-overview.md) | 26–      |
| agent-workspace-customer-ticket-navigation | [agent-workspace-customer-ticket-navigation/00-overview.md](./agent-workspace-customer-ticket-navigation/00-overview.md) | 27–      |
| agent-workspace-real-dashboard | [agent-workspace-real-dashboard/00-overview.md](./agent-workspace-real-dashboard/00-overview.md) | 28–      |
| agent-workspace-unassigned-tickets | [agent-workspace-unassigned-tickets/00-overview.md](./agent-workspace-unassigned-tickets/00-overview.md) | 29–      |
| agent-workspace-customer-editing | [agent-workspace-customer-editing/00-overview.md](./agent-workspace-customer-editing/00-overview.md) | 30–      |
| agent-workspace-sla-policy-admin | [agent-workspace-sla-policy-admin/00-overview.md](./agent-workspace-sla-policy-admin/00-overview.md) | 31–      |
| agent-workspace-user-admin | [agent-workspace-user-admin/00-overview.md](./agent-workspace-user-admin/00-overview.md) | 32–      |
| agent-workspace-business-hours-admin | [agent-workspace-business-hours-admin/00-overview.md](./agent-workspace-business-hours-admin/00-overview.md) | 33–      |
| agent-workspace-roles-permissions-viewer | [agent-workspace-roles-permissions-viewer/00-overview.md](./agent-workspace-roles-permissions-viewer/00-overview.md) | 34–      |
| identity-branch-department-listing | [identity-branch-department-listing/00-overview.md](./identity-branch-department-listing/00-overview.md) | 35–      |
| notifications-read-endpoint | [notifications-read-endpoint/00-overview.md](./notifications-read-endpoint/00-overview.md) | 36–      |
| audit-log-read-endpoint | [audit-log-read-endpoint/00-overview.md](./audit-log-read-endpoint/00-overview.md) | 37–      |
| _(unplanned)_ agent-workspace-user-admin / notification-history / audit-log-viewer follow-ups | Stories 38 (Create User), 39 (Notification History), 40 (Audit Log Viewer) — implemented directly from user-supplied specifications; no `.squad` plan/story representation exists for them (same convention as Story 24) | 38–40    |
| agent-workspace-session-refresh | [agent-workspace-session-refresh/00-overview.md](./agent-workspace-session-refresh/00-overview.md) | 41–      |
| agent-workspace-ticket-detail-reassignment | [agent-workspace-ticket-detail-reassignment/00-overview.md](./agent-workspace-ticket-detail-reassignment/00-overview.md) | 42–      |
| agent-workspace-ticket-creation-fields | [agent-workspace-ticket-creation-fields/00-overview.md](./agent-workspace-ticket-creation-fields/00-overview.md) | 43–      |
| agent-workspace-navigation-menu | [agent-workspace-navigation-menu/00-overview.md](./agent-workspace-navigation-menu/00-overview.md) | 44–      |
| agent-workspace-branch-department-admin | [agent-workspace-branch-department-admin/00-overview.md](./agent-workspace-branch-department-admin/00-overview.md) | 45–      |
| agent-workspace-role-permission-management | [agent-workspace-role-permission-management/00-overview.md](./agent-workspace-role-permission-management/00-overview.md) | 46–      |
| agent-workspace-user-role-assignment | [agent-workspace-user-role-assignment/00-overview.md](./agent-workspace-user-role-assignment/00-overview.md) | 47–      |
| agent-workspace-user-profile-correction | [agent-workspace-user-profile-correction/00-overview.md](./agent-workspace-user-profile-correction/00-overview.md) | 48–      |
| agent-workspace-sla-escalation-visibility | [agent-workspace-sla-escalation-visibility/00-overview.md](./agent-workspace-sla-escalation-visibility/00-overview.md) | 49–      |
| agent-workspace-ticket-internal-notes | [agent-workspace-ticket-internal-notes/00-overview.md](./agent-workspace-ticket-internal-notes/00-overview.md) | 50–      |
| knowledge-base-foundation | [knowledge-base-foundation/00-overview.md](./knowledge-base-foundation/00-overview.md) | 51–      |
| customer-portal-authentication-foundation | [customer-portal-authentication-foundation/00-overview.md](./customer-portal-authentication-foundation/00-overview.md) | 52–      |
| customer-portal-ticket-submission-tracking | [customer-portal-ticket-submission-tracking/00-overview.md](./customer-portal-ticket-submission-tracking/00-overview.md) | 53–      |
| customer-portal-knowledge-base-browsing | [customer-portal-knowledge-base-browsing/00-overview.md](./customer-portal-knowledge-base-browsing/00-overview.md) | 54–      |
| customer-portal-ticket-csat-feedback | [customer-portal-ticket-csat-feedback/00-overview.md](./customer-portal-ticket-csat-feedback/00-overview.md) | 55–      |
| reporting-analytics-foundation | [reporting-analytics-foundation/00-overview.md](./reporting-analytics-foundation/00-overview.md) | 56–      |
| sla-automation-rules | [sla-automation-rules/00-overview.md](./sla-automation-rules/00-overview.md) | 57–      |
| notification-preferences | [notification-preferences/00-overview.md](./notification-preferences/00-overview.md) | 58–      |
| reporting-agent-performance | [reporting-agent-performance/00-overview.md](./reporting-agent-performance/00-overview.md) | 59–      |
| reporting-ticket-aging | [reporting-ticket-aging/00-overview.md](./reporting-ticket-aging/00-overview.md) | 60–      |
| notification-templates | [notification-templates/00-overview.md](./notification-templates/00-overview.md) | 61–      |
| admin-branch-branding | [admin-branch-branding/00-overview.md](./admin-branch-branding/00-overview.md) | 62–      |
| notification-templates-live-toast | [notification-templates-live-toast/00-overview.md](./notification-templates-live-toast/00-overview.md) | 63–      |
| knowledge-base-article-search | [knowledge-base-article-search/00-overview.md](./knowledge-base-article-search/00-overview.md) | 64–      |
| knowledge-base-article-versioning | [knowledge-base-article-versioning/00-overview.md](./knowledge-base-article-versioning/00-overview.md) | 65–      |
| ticket-attachments | [ticket-attachments/00-overview.md](./ticket-attachments/00-overview.md) | 66–      |
| customer-attachments | [customer-attachments/00-overview.md](./customer-attachments/00-overview.md) | 67–      |
| ticket-department-visibility | [ticket-department-visibility/00-overview.md](./ticket-department-visibility/00-overview.md) | 68–      |
| _(unplanned)_ ticket-department-scoped-reassignment | Story 69 — implemented directly as a low-ambiguity extension of Story 68's already-approved `isDepartmentScopedCaller` mechanism (`PATCH /tickets/:id` rejects a department reassignment for a department-scoped caller); no formal `.squad` plan artifact per `CLAUDE.md` §3.4's "self-evidently small" allowance (stated explicitly in the Story 69 commit message itself). This row was reconstructed during the Stories-69+ planning reconciliation — the story existed correctly in git history but had no `00-index.md` row at all until now. | 69        |
| _(unplanned)_ ticket-search-foundation | Story 70 — implemented directly from an approved Recon recommendation; no formal `.squad` plan artifact (self-evidently small, `CLAUDE.md` §3.4) | 70        |
| _(unplanned)_ realtime-agent-presence | Story 71 — implemented directly after an approved batching check; no formal `.squad` plan artifact (self-evidently small extension of `realtime-socketio-foundation`, `CLAUDE.md` §3.4) | 71        |
| ai-services-foundation | [ai-services-foundation/00-overview.md](./ai-services-foundation/00-overview.md) | 72–      |
| _(unplanned)_ ticket-ai-summarization | Story 73 — implemented directly as a self-evidently small first consumer of `ai-services-foundation`'s `AiGatewayService` (`CLAUDE.md` §3.4); no formal `.squad` plan artifact | 73        |
| _(unplanned)_ ticket-ai-suggested-reply | Story 74 — implemented directly as an additive extension of Story 73's `TicketAiService` (`CLAUDE.md` §3.4); no formal `.squad` plan artifact | 74        |
| _(unplanned)_ ticket-ai-categorization | Story 75 — implemented directly as an additive extension of Story 73's `TicketAiService` (`CLAUDE.md` §3.4); no formal `.squad` plan artifact | 75        |
| _(unplanned)_ shared-ai-provider-boundary | Architecture-boundary refactor (`07f896a`) — extracted `packages/ai` (`@crm/ai`); committed as `refactor(ai): ...`, not assigned a story number (no product-facing behavior change) | —         |
| ai-ticket-assist-async-processing | [ai-ticket-assist-async-processing/00-overview.md](./ai-ticket-assist-async-processing/00-overview.md) | 76–      |
| _(unplanned)_ env-defaults-maintenance | `feat(env): add local MinIO defaults for S3 settings and corresponding tests` (`fbffaab`) — plain maintenance commit, not assigned a story number (no product-facing behavior change) | —         |
| customer-portal-live-chat | [customer-portal-live-chat/00-overview.md](./customer-portal-live-chat/00-overview.md) | 77–      |
| ai-ticket-assist-result-delivery | [ai-ticket-assist-async-processing/79-story-ai-ticket-assist-result-delivery.md](./ai-ticket-assist-async-processing/79-story-ai-ticket-assist-result-delivery.md) | 79–      |
| ai-portal-chatbot | [ai-portal-chatbot/00-overview.md](./ai-portal-chatbot/00-overview.md) | 80–      |
| ai-feature-flags | [ai-feature-flags/00-overview.md](./ai-feature-flags/00-overview.md) | 81–      |
| admin-branch-branding-live-consumption | [admin-branch-branding-live-consumption/00-overview.md](./admin-branch-branding-live-consumption/00-overview.md) | 82–      |
| sla-automation-rules-category-department | [sla-automation-rules-category-department/00-overview.md](./sla-automation-rules-category-department/00-overview.md) | 83–      |
| identity-audit-logging | [identity-audit-logging/00-overview.md](./identity-audit-logging/00-overview.md) | 84–      |
| ai-chat-escalation-to-ticket | [ai-chat-escalation-to-ticket/00-overview.md](./ai-chat-escalation-to-ticket/00-overview.md) | 85–      |
| customer-portal-notification-delivery | [customer-portal-notification-delivery/00-overview.md](./customer-portal-notification-delivery/00-overview.md) | 86–      |
| channels-web-form-intake | [channels-web-form-intake/00-overview.md](./channels-web-form-intake/00-overview.md) | 87–      |
| customer-portal-notification-history | [customer-portal-notification-history/00-overview.md](./customer-portal-notification-history/00-overview.md) | 88–      |
| customer-portal-notification-history-frontend | [customer-portal-notification-history-frontend/00-overview.md](./customer-portal-notification-history-frontend/00-overview.md) | 89–      |
| customer-portal-notification-preferences | [customer-portal-notification-preferences/00-overview.md](./customer-portal-notification-preferences/00-overview.md) | 90–      |
| channels-quick-replies | [channels-quick-replies/00-overview.md](./channels-quick-replies/00-overview.md) | 91–      |
| notification-read-state | [notification-read-state/00-overview.md](./notification-read-state/00-overview.md) | 92–      |
| _(unplanned)_ reporting-date-range-filtering | Story 93 (`56ea226`) — "add reporting date-range filtering"; no formal `.squad` plan artifact. This row was reconstructed during the Plan Index Backfill (backlog #10) — the story existed correctly in git history but had no `00-index.md` row at all until now. | 93        |
| _(unplanned)_ frontend-feedback-standardization | Story 94 (`891e9f4`) — "standardize frontend feedback and confirmations"; no formal `.squad` plan artifact. Reconstructed during the Plan Index Backfill. | 94        |
| _(unplanned)_ auth-failure-recovery | Story 95 (`05e3d6c`) — "recover from unrecoverable auth failures instead of stranding the user"; no formal `.squad` plan artifact. Reconstructed during the Plan Index Backfill. | 95        |
| _(unplanned)_ navigation-and-error-routes-polish | Story 96 (`63de3e9`) — "make navigation and unmatched routes production-quality"; no formal `.squad` plan artifact. Reconstructed during the Plan Index Backfill. | 96        |
| _(unplanned)_ loading-state-skeletons | Story 97 (`25804a9`) — "shape loading states to their real layout, not generic bars"; no formal `.squad` plan artifact. Reconstructed during the Plan Index Backfill. | 97        |
| _(unplanned)_ visual-polish-and-a11y | Story 98 (`6149921`) — "standardize status color, destructive actions, and a11y polish"; no formal `.squad` plan artifact. Reconstructed during the Plan Index Backfill. | 98        |
| reporting-resolution-time-metrics | [reporting-resolution-time-metrics/00-overview.md](./reporting-resolution-time-metrics/00-overview.md) | 99–      |
| identity-security-hardening | [identity-security-hardening/00-overview.md](./identity-security-hardening/00-overview.md) | 100–     |
| customer-list-search | [customer-list-search/00-overview.md](./customer-list-search/00-overview.md) | 101–     |
| knowledge-base-fulltext-search | [knowledge-base-fulltext-search/00-overview.md](./knowledge-base-fulltext-search/00-overview.md) | 102–     |
| portal-ticket-attachments | [portal-ticket-attachments/00-overview.md](./portal-ticket-attachments/00-overview.md) | 103–     |
| audit-log-search | [audit-log-search/00-overview.md](./audit-log-search/00-overview.md) | 104–     |
| ticket-list-cap | [ticket-list-cap/00-overview.md](./ticket-list-cap/00-overview.md) | 105–     |
| bounded-list-caps | [bounded-list-caps/00-overview.md](./bounded-list-caps/00-overview.md) | 106–     |
| branch-creation | [branch-creation/00-overview.md](./branch-creation/00-overview.md) | 107–     |
| agent-presence-ui | [agent-presence-ui/00-overview.md](./agent-presence-ui/00-overview.md) | 108–     |
| kb-multi-locale | [kb-multi-locale/00-overview.md](./kb-multi-locale/00-overview.md) | 109–     |
| reporting-saved-dashboards | [reporting-saved-dashboards/00-overview.md](./reporting-saved-dashboards/00-overview.md) | 110–     |
| structured-logging | [structured-logging/00-overview.md](./structured-logging/00-overview.md) | 111–     |
| observability-tracing-metrics | [observability-tracing-metrics/00-overview.md](./observability-tracing-metrics/00-overview.md) | 112–     |
| error-tracking | [error-tracking/00-overview.md](./error-tracking/00-overview.md) | 113–     |
| playwright-e2e | [playwright-e2e/00-overview.md](./playwright-e2e/00-overview.md) | 114–     |
| audit-log-db-grants | [audit-log-db-grants/00-overview.md](./audit-log-db-grants/00-overview.md) | 115–     |
| ai-chat-conversation-memory | [ai-chat-conversation-memory/00-overview.md](./ai-chat-conversation-memory/00-overview.md) | 116–     |
| ai-chat-kb-grounding | [ai-chat-kb-grounding/00-overview.md](./ai-chat-kb-grounding/00-overview.md) | 117–     |
| identity-multi-branch-assignment | [identity-multi-branch-assignment/00-overview.md](./identity-multi-branch-assignment/00-overview.md) | 118–     |
| locale-preference | [locale-preference/00-overview.md](./locale-preference/00-overview.md) | 119–     |
| ticket-category-taxonomy | [ticket-category-taxonomy/00-overview.md](./ticket-category-taxonomy/00-overview.md) | 120–     |
| _(unplanned)_ reporting-ai-usage-cost | Story 121 (`d5eb59c`) — "add AI usage/cost reporting"; no formal `.squad` plan artifact per `CLAUDE.md` §3.4's "self-evidently small" allowance (an additive `ReportingService` method mirroring six existing report endpoints' own shape). Reconstructed during the Plan Index Backfill. | 121       |
| _(unplanned)_ identity-account-lockout | Story 122 (`c7a5a58`) — "add per-account login lockout after repeated failed attempts"; no formal `.squad` plan artifact (closes Story 100's own disclosed non-goal). Reconstructed during the Plan Index Backfill. | 122       |
| _(unplanned)_ identity-password-complexity | Story 123 (`b79725c`) — "add password complexity requirements"; no formal `.squad` plan artifact. Reconstructed during the Plan Index Backfill. | 123       |
| _(unplanned)_ identity-session-device-management | Story 124 (`bcf74bd`) — "add session/device management"; no formal `.squad` plan artifact (closes the "sessions" noun in `03-domain-boundaries.md`'s Identity & Access row). Reconstructed during the Plan Index Backfill. | 124       |
| _(unplanned)_ reporting-export | Story 125 (`6ced7f8`) — "add CSV export to every reporting endpoint"; no formal `.squad` plan artifact (a single reusable mechanism applied uniformly across seven already-modeled reports). Reconstructed during the Plan Index Backfill. | 125       |
| _(unplanned)_ plan-index-backfill | Reconstructs the 33 missing `00-index.md` rows above (Stories 93–125) — a mix of directories with a full `.squad/plans/**` artifact that were simply never indexed, and directly-implemented "(unplanned)" stories — all of which existed correctly in git history/on disk but had no index row at all until now. Closes backlog item #10 ("Plan Index Backfill"). Plain documentation/bookkeeping commit, not assigned a story number (no product-facing behavior change). | —         |
| _(unplanned)_ reporting-ticket-volume-by-category | Story 126 — "Ticket Volume by Category," a `ReportingService` report + CSV export + `ReportWidgetType` dashboard widget, grouping `Ticket.categoryId` (the managed vocabulary Story 120 introduced, replacing free-text `category`). No formal `.squad` plan artifact (self-evidently small — mirrors `getTicketVolumeByStatus`/`getAgentPerformance`'s existing shape field-for-field; CLAUDE.md §3.4). Selected because Story 120's own plan explicitly named this as the first report this new FK unblocks. | 126       |
| presence-stale-agent-session-expiration | [presence-stale-agent-session-expiration/00-overview.md](./presence-stale-agent-session-expiration/00-overview.md) | Presence stale agent session expiration | 127       |
| ticket-list-default-sort-and-newest-ticket-discoverability | [ticket-list-default-sort-and-newest-ticket-discoverability/00-overview.md](./ticket-list-default-sort-and-newest-ticket-discoverability/00-overview.md) | Story 128 (`09f08ff`) — "default ticket list to newest first." The plan folder's own `00-overview.md` was never filled in beyond its template placeholder; the story shipped directly. Reconstructed during this finalization documentation pass — it existed correctly in git history but had no `00-index.md` row until now. | 128       |
| _(unplanned)_ nav-1 | Commit `04bb9a2` — "handle branch-switch failures instead of throwing unhandled": `WorkspaceNav`'s branch switcher now catches a failed switch and surfaces it, instead of silently leaving the session on the same branch with no feedback. No formal `.squad` plan artifact (self-evidently small, `CLAUDE.md` §3.4). Reconstructed during this finalization pass. | —         |
| _(unplanned)_ portal-1 | Commit `c636426` — "paginate the portal's my tickets list": applies the existing `paginate()`/id-tie-breaker convention to the one list endpoint Story 105 explicitly deferred (`GET /portal/tickets`). No formal plan artifact. Reconstructed during this finalization pass. | —         |
| _(unplanned)_ a11y-2 | Commit `4fe125c` — "label every Radix Select and set aria-sort on sortable headers." No formal plan artifact. Reconstructed during this finalization pass. | —         |
| _(unplanned)_ portal-2 | Commit `a8a4039` — "paginate the portal's notification history and drop its raw table," adopting the shared `Table`/pagination convention for the one remaining unbounded portal list. No formal plan artifact. Reconstructed during this finalization pass. | —         |
| _(unplanned)_ ds-1a | Commit `76b3598` — "extract the shared Tailwind theme into @crm/config." No formal plan artifact. Reconstructed during this finalization pass. | —         |
| _(unplanned)_ nav-2 | Commit `51b8db0` — "add a heading landmark to the three detail views missing one." No formal plan artifact. Reconstructed during this finalization pass. | —         |
| _(unplanned)_ ds-1b | Commit `7f42fb8` — "extract the shared globals.css tokens into @crm/config," continuing `ds-1a`'s token consolidation. No formal plan artifact. Reconstructed during this finalization pass. | —         |
| _(unplanned)_ nav-2b | Commit `7220419` — "cap the agent workspace shell's content width." No formal plan artifact. Reconstructed during this finalization pass. | —         |
| _(unplanned)_ a11y-3 | Commit `b0eac6e` — "add a skip-to-main-content link to both apps' shells" — the last commit before `core-completion-roadmap`'s planning documents were written (they cite this exact hash as their checkpoint). No formal plan artifact. Reconstructed during this finalization pass. | —         |
| _(unplanned)_ core-completion-roadmap (planning only) | [core-completion-roadmap/00-overview.md](./core-completion-roadmap/00-overview.md) — commit `6a50c3b docs(core-completion-roadmap): add dependency-aware core completion roadmap`. Planning-only (no code/schema/config change): re-verified the repository against a prior Core Features Recon and proposed `RM-00`…`RM-25` as future stories, each given its own `RM-NN-*.md` file in this folder. Per this document's own "Numbering" section, `RM-xx` was declared a namespace distinct from the real global `NN` sequence, with each story meant to "earn its real, next-available `NN`" once actually implemented. In practice (see the row below and the already-indexed RM-26–30 rows above), every `RM-NN` story instead shipped under a `feat(rm-NN): ...` commit message and kept its `RM-` label rather than being renumbered — noted here as a documentation-vs-practice fact, not corrected. Reconstructed during this finalization pass. | —         |
| core-completion-roadmap | [core-completion-roadmap/00-overview.md](./core-completion-roadmap/00-overview.md) | `RM-00` through `RM-25` — 26 stories, each with its own formal `RM-NN-*.md` plan file in this folder, all implemented as `feat(rm-NN): ...` commits, e.g. `50bdf22 feat(rm-00): add AI Suggested Solutions`, `ca5cc31 feat(rm-01): centralize ticket status transition policy`, `98c6775 feat(rm-02): add Customer Notes`, `7f7a572 feat(rm-03): add Agent Tasks & Reminders`, `26612cf feat(rm-04): add an embedded customer context panel`, `b45d236 feat(rm-05): link tickets to Knowledge Base articles`, `3e4f330 feat(rm-06): add workspace presence to the assignee picker and @mentions`, `ab3e9bf feat(rm-07): add cross-dimension report filters and a manager rollup`, `0950783 feat(rm-08): render reports as charts`, `ef1f481 feat(rm-09): resolve Dashboard naming collision`, `e128d4b feat(rm-10): make data tables mobile-responsive`, `19b39b2 feat(rm-11): collapse workspace/portal nav behind a mobile menu`, `deb8bb1 feat(rm-12): add locale routing/switching test coverage`, `6985940 feat(rm-13): add a channel message delivery status/retry model`, `735cc93 feat(rm-14): formalize a channel adapter interface and registry`, `782b0e9 feat(rm-15): add outbound email channel adapter`, `25a37f6 feat(rm-19): add portal email notification delivery`, `15732eb feat(rm-20): add webhook subscriptions and outbound event dispatch`, `824f581 feat(rm-21): add inbound webhook receiver and signature verification`, `bafc128 feat(rm-22): add api-key authentication`, `e15603a feat(rm-23): add consolidated system settings screen`, `d856837 feat(rm-25): add sla pause/resume on-hold clock`, `96f043d feat(rm-24): add load-balanced automation rule assignment` (RM-24 shipped chronologically after RM-25). No formal `.squad/plans/00-index.md` rows existed for any of RM-00–RM-25 until this finalization pass, despite each having a real plan file — reconstructed here. | RM-00–RM-25 |
| _(unplanned)_ ux-hardening-batch-6-8 | Recon-driven UX/responsiveness/RTL/a11y hardening batches, each a `fix(ux)`/`feat(ux)`/`refactor(ux)` commit with no individual formal plan artifact: `94cc8ae` "centralize error classification across remaining raw-message call sites", `02d4704` "fix webhook log pagination flash and close the KB article route-transition gap", `55ad4b9` "group workspace navigation, drop duplicate settings entries, add back-links", `56f9b2c` "persist list filters/sort/page in the URL; paginate Users; add KB category filter", `c893ddf` "revert blur-commit fields on error; confirm every ticket field update", `608b7ae` "adopt shared Checkbox/Select/QueryStateCard over hand-rolled equivalents", `b71f2c5` "pool realtime connections, surface reconnects, dedupe toasts", `27a2373` "make Tabs/Dialog overflow-safe, fix one RTL leak, adopt Checkbox in 3 more forms" (self-labeled "Recon's Batch 8"). Reconstructed during this finalization pass. | —         |
| _(unplanned)_ readme-env-and-domain-doc-fixes | Commit `fa3e2bc docs: fix README env-var/e2e-count drift, document health checks, backfill RM-26..30` (self-labeled "Recon's Batch 9") — documentation-only: added missing env vars to README's table, corrected the e2e spec count (33→54), documented the `/health`/`/health/ready` routes, added the missing `Tasks` row to `03-domain-boundaries.md`, and backfilled the RM-26–30 rows above. No code/schema change. | —         |
| _(unplanned)_ workspace-navigation-and-realtime-polish | Post-Batch-9 UX/navigation/realtime polish, each a distinct commit with no individual formal plan artifact: `b49f182` "add global navigation and button pending loading" (route-loading skeletons to 33 previously-uncovered routes, per-action AI-button loading state), `34e9e7f` "polish workspace navigation" (icons, grouped desktop nav, semantic color tokens), `8445e8b` "close login and route-navigation loading gaps at the auth-layout boundary", `9cd542e` "isolate e2e test database from local dev database" (a real fix — the e2e suite had been resetting the developer's own dev DB), `02a44bc` "implement NavigationOverlayListener for client-side navigation feedback", `9d03607` "handle response errors in browser-check-temp and update ESLint config for .mjs files", `8c2b0b5` "update getMyTicketCsat to return null for 204 responses", `09a3328` "update mock return value for useMyTicketCsatQuery... adjust conditional check in CsatSection" (the CSAT 204-handling fix, in the API client and its consuming component respectively). Reconstructed during this finalization pass. | —         |
| ~~slabusiness-hours-awaretargetcomputation~~ | [slabusiness-hours-awaretargetcomputation/00-overview.md](./slabusiness-hours-awaretargetcomputation/00-overview.md) — a stray, mistakenly-hyphenated plan-intake scaffold (`45327fe`, intended name "sla-business-hours-aware-target-computation") whose `00-overview.md` was never filled in beyond its template placeholder. Its actual story — business-hours-aware SLA target computation — was implemented directly as **Story 13**, already covered by `sla-policy-foundation`'s own open-ended `10–` range above. Noted here (rather than silently left unindexed) so this Plan Index Backfill's own cross-check of "every directory under `.squad/plans/` appears in this index" stays true; the directory itself is left as-is, not deleted, since it predates this Story and holds no content to lose. | —         |
| ~~test~~ | [test/00-overview.md](./test/00-overview.md) — an empty scaffold from the very first commit (`89dc1fe`, project bootstrapping/squad-kit tooling setup), never filled in beyond its template placeholder and never used for a real story. Noted here for the same completeness reason as the row above; left as-is. | —         |
| _(unplanned)_ rm-26-agent-email-notification-delivery | Story RM-26 (`7533b27`) — "add agent email notification delivery": SLA-at-risk/breach and ticket-escalation listeners enqueue an email via the existing `EmailAdapter` (RM-15) alongside their existing in-app notification. No formal `.squad/plans/core-completion-roadmap/RM-26-*.md` artifact — the roadmap's own per-story files stop at RM-25; this and the four rows below continue the autonomous Story loop past that point per `CLAUDE.md` §1/§8. Reconstructed during this Recon's own docs-drift pass (Batch 9). | RM-26     |
| _(unplanned)_ rm-27-knowledge-base-category-taxonomy | Story RM-27 (`61a5b57`) — "add knowledge base category taxonomy": a new `KnowledgeBaseCategory` model/CRUD replacing free-text `category` on `KnowledgeBaseArticle` (`categoryId`), mirroring `TicketCategory`'s own managed-vocabulary shape (Story 120). No formal plan artifact; reconstructed during Batch 9. | RM-27     |
| _(unplanned)_ rm-28-kb-article-attachments | Story RM-28 (`f86490b`) — "add knowledge base article attachments": extends the existing `AttachmentsCard`/`AttachmentsService` (Stories 66/67) to a third owner type, `kb-article`. No formal plan artifact; reconstructed during Batch 9. | RM-28     |
| _(unplanned)_ rm-29-automation-rule-priority-action | Story RM-29 (`6345323`) — "add automation rule auto-set priority action": automation rules can now also set a ticket's priority on creation (alongside the existing category/department/owner actions), guarded by the same "only when the field is still unset" rule the other actions already follow. No formal plan artifact; reconstructed during Batch 9. | RM-29     |
| _(unplanned)_ rm-30-locale-aware-notification-templates | Story RM-30 (`2ebb4c3`) — "add locale-aware notification templates": `NotificationTemplate` gains an optional `locale`, letting an admin layer an English/Arabic override on top of (never replacing) a branch's own default template per event type. No formal plan artifact; reconstructed during Batch 9. | RM-30     |
| admin-branding-navigation | [admin-branding-navigation/00-overview.md](./admin-branding-navigation/00-overview.md) | Admin Branding & Navigation Layout Customization — extends Story 62's `BrandingConfig` row with `appName` + `navigationLayout` (Sidebar/Navbar), extracts `NAV_GROUPS` into a single shared source of truth, and renders it through two first-class presentations behind an admin setting. | 129       |
| _(unplanned)_ notification-template-lifecycle | Story 130 (`4481f74`) — "add notification template lifecycle": `NotificationTemplate` gains `isActive`, with an admin activate/deactivate toggle mirroring `AutomationRulesView`'s own, and deliberately no hard `DELETE`. Story 131 (`236e5e8`) — "honour inactive notification templates": the three frontend consumers skip inactive rows while building their template maps, so a deactivated template falls back to its built-in copy. No formal plan artifacts (self-evidently small, `CLAUDE.md` §3.4). | 130–131   |
| _(unplanned)_ p1-2-playwright-cross-surface-journeys | Commit `04dd36d test(e2e): add critical cross-surface journeys` — three Playwright journeys covering behaviour only a real browser spanning two apps can prove: client-side session expiry/refresh recovery, Knowledge Base draft→published portal visibility, and agent↔customer live chat over the real Socket.IO `ticket:{id}` room. Planned under the `P1-2` label rather than an `NN`; no formal plan artifact. | —         |
| machine-facing-reporting-api-key | [machine-facing-reporting-api-key/00-overview.md](./machine-facing-reporting-api-key/00-overview.md) | Machine-facing reporting endpoint authenticated by API key — registers RM-22's FIRST real production route, `GET /integrations/reports/ticket-volume` (read-only JSON, `integration:read` scope, branch-confined). A separate controller rather than a decorator on `/reports/*`, because `PermissionsGuard` runs before `ApiKeyGuard` so a route cannot carry both `@RequirePermissions(...)` and `@AllowApiKey()`. No existing route, guard, or guard ordering changed. | 133       |
| customer-data-anonymization | [customer-data-anonymization/00-overview.md](./customer-data-anonymization/00-overview.md) | Customer Data Anonymization / Right-to-Erasure — adds `anonymizedAt` to `Customer`/`Contact`, a dedicated `customer:anonymize` permission, and a transactional `POST /customers/:id/anonymize` that clears structured identity data and portal access **in place**. Hard deletion is deliberately not implemented: `tickets.customer_id` is `RESTRICT NOT NULL` and six related relations are `CASCADE`, so ticket/SLA/reporting history is preserved instead. Free-text bodies, stored S3 objects and the immutable audit log are documented as retained. | 132       |
| design-system-token-foundation | [design-system-token-foundation/00-overview.md](./design-system-token-foundation/00-overview.md) | Design System — Token Foundation (DS-A). Extends the existing token mechanism beyond colour: semantic **spacing**, **radius** and **elevation** tokens added to `tailwind-tokens.css` and exposed additively through `tailwind-preset.js`, alongside the existing `colors`/`fontFamily`/`fontSize`. A pure vocabulary story — it adds names, it does not apply them, and no `.tsx` is touched. First of the UI/UX redesign sequence approved from the recon at `2f7aec5`; blocks DS-B..DS-E. | 134       |
| portal-adopts-shared-ui-primitives | [portal-adopts-shared-ui-primitives/00-overview.md](./portal-adopts-shared-ui-primitives/00-overview.md) | Portal adopts the shared UI primitives (DS-B'). The portal's 20 hand-rolled raw-red error boxes across 10 files become `<Alert variant="destructive">` (4 of them pairing a `<Button variant="outline" size="sm">` retry), its 3 raw `<textarea>` become `<Textarea>`, and `apps/portal/src/design-tokens.spec.ts` gains a focused guard against the raw-red pattern returning. A pure primitive-adoption story — no behaviour, copy, navigation, auth or realtime change, no `apps/web` or `packages/ui` file touched. Planning re-measured the intake's inventory and corrected two claims: the raw `<table>` no longer exists (already migrated by PORTAL-2; the intake's `grep -l` matched doc-comment prose), and the error box is 20 occurrences rather than the 10 files the intake counted. | 135       |
| portal-home-dashboard | [portal-home-dashboard/00-overview.md](./portal-home-dashboard/00-overview.md) | Portal Home — replaces the Customer Portal's canonical landing page, a 28-line Story-52/53 placeholder whose i18n key is literally named `placeholder`, with a real one: a welcome block, a ticket panel (the paginated envelope's accurate `total` plus the 5 most recent, status-badged via the unmodified `ticketStatusBadgeVariant`), a knowledge-base panel (5 published articles), and navigation to the four existing destinations. Thin server page + client view, per-panel loading/empty/error, errors through Story 135's shared `Alert`. Planning rejected two candidate features on evidence: a status breakdown (no status filter exists on `GET /portal/tickets` and `DEFAULT_PAGE_SIZE` is 25, so page-1 counts would be silently wrong past 25 tickets) and an unread-notification tile (`PortalHeader` already shows it on every page). No backend change, no `PortalHeader` change, no new route, no new dependency. | 136       |
| add-arabic-locale-tab-editing-for-knowledge-base-articles | [add-arabic-locale-tab-editing-for-knowledge-base-articles/00-overview.md](./add-arabic-locale-tab-editing-for-knowledge-base-articles/00-overview.md) | Knowledge Base Arabic locale-tab editing — Story 109 shipped the translation backend in full (`PUT`/`GET .../translations`, the `KnowledgeBaseArticleTranslation` model, locale resolution with base-content fallback) and deliberately stopped short of an authoring UI; a repo-wide search for `translations` in `apps/web/src`/`apps/portal/src` returns zero hits, so no agent can author Arabic KB content through the product. Adds English/Arabic tabs to `ArticleDetailView` using the existing shared `Tabs`, the missing API client functions, a separately-rooted translations query key and its query/mutation hooks. Arabic saves explicitly (both fields together) because the `PUT` contract replaces wholesale, unlike the base editor's partial-PATCH blur-commit, which is unchanged. Frontend-only: no endpoint, DTO, Prisma, permission or fallback change, and no Docker needed to verify. | 137       |
| add-arabic-knowledge-base-full-text-search | [add-arabic-knowledge-base-full-text-search/00-overview.md](./add-arabic-knowledge-base-full-text-search/00-overview.md) | Arabic Knowledge Base full-text search — Story 109 deferred Arabic-aware search to "its own future story"; Story 137 then shipped Arabic authoring, so Arabic content now exists and cannot be found (`search_vector` is `to_tsvector('english', …)` over the base article only, and the translations table is unindexed — measured: 0 hits for `الدعم` against the live container). Adds locale-routed Arabic FTS over `AR` translation rows using PostgreSQL 16's built-in `arabic` configuration (core `arabic_stem` Snowball dictionary, no extension), backed by one hand-written partial expression GIN index whose usability was proven by `EXPLAIN` at 20k rows inside a rolled-back transaction. A sibling `searchArticlesInArabic` helper; the eight existing English raw SQL blocks and Story 102's generated column are untouched. Backend only — no DTO, controller, schema.prisma or frontend change. | 138       |

| form-field-accessibility | [form-field-accessibility/00-overview.md](./form-field-accessibility/00-overview.md) | Form Field Accessibility — `FormField` renders `hint`/`error` inside its `<label>`, so both are measured to land in the control's accessible name (`"SubjectKeep it short.Required"`; exact `getByLabelText("Subject")` fails), and the control gets neither `aria-invalid` nor an `aria-describedby` pointing at the message. Moves the hint/error out of the `<label>` into a wrapper, generates ids with `React.useId` (the repo's first use), and injects `aria-invalid`/`aria-describedby` onto the child via `cloneElement` — merging rather than overwriting any value a consumer already set. Confined to `packages/ui`; none of the 7 call sites is edited, and `branding-view.tsx` (not a `FormField` consumer) is untouched. | 151 |
| design-system-foundation | [design-system-foundation/00-overview.md](./design-system-foundation/00-overview.md) | Design System Foundation — **already delivered; planned as a no-op**. The intake describes the tree before Stories 134/135/139/142; all six of its Description claims were re-measured at `ecbb6ba` and none holds (`Card` 65 usages not "no consumers"; 0 hand-rolled surface strings; `EmptyState` 14 usages; 0 raw red error boxes in the portal). Both foundations are already enforced by `apps/web/src/design-tokens.spec.ts` (Card surfaces) and `apps/portal/src/design-tokens.spec.ts` (Alert error boxes). No equivalent-and-safe migration remains: the 12 residual raw containers are auth/error page shells, a dropdown, a toast and `bg-surface-sunk` notices — all named as intentional exclusions by the guard itself — and `Card`'s unused sub-components plus `QueryStateCard`'s absent wrapper are documented Story 139 decisions. Plan recommends closing the story or re-scoping with new evidence. | 152 |

| restore-focus-and-strengthen-menu-focus-indicators | [restore-focus-and-strengthen-menu-focus-indicators/00-overview.md](./restore-focus-and-strengthen-menu-focus-indicators/00-overview.md) | Restore focus and strengthen menu focus indicators — two defects from one read-only recon at HEAD `042111a`. (1) Four controls unmount themselves while focused and leave focus on `document.body`: the three inline-edit `Input`s (ticket subject, customer display name, KB article title) on their Escape and Enter/save exits, and the Reports "Save current view" form's own Save and Cancel buttons. Each gains an explicit restore to its persistent trigger, guarded on `document.activeElement === document.body` so a blur caused by clicking another control is never overridden — inline per file, following `ConfirmDialog`'s (Story 94) hand-rolled `useRef` + `.focus()` precedent, with no new focus-management hook. (2) `menuItemClassName` indicated focus only with `focus:bg-surface-muted`, which measures **1.10:1** between `--surface` (255 255 255) and `--surface-muted` (241 245 249) — under WCAG 2.4.11's 3:1 threshold; it adopts the existing `.focus-ring-always`, whose own doc comment already names menu items as its intended consumer. No token added or changed, no `packages/ui` component edited (`DropdownMenuItem`/`SelectItem` inherit; `Popover` unaffected), no backend/i18n/RBAC change. Portal CSAT radiogroup keyboard behaviour (recon finding 4) deliberately deferred as a separate ARIA composite-widget concern. | 166 |
| give-the-portal-csat-rating-the-aria-radio-group-keyboard-pattern | [give-the-portal-csat-rating-the-aria-radio-group-keyboard-pattern/00-overview.md](./give-the-portal-csat-rating-the-aria-radio-group-keyboard-pattern/00-overview.md) | Give the portal CSAT rating the ARIA radio-group keyboard pattern — closes the last finding from the recon that produced Story 166, which deferred it as a separate ARIA composite-widget concern. The portal CSAT rating is a hand-built `div role="radiogroup"` over five `button role="radio"` with correct `aria-label`/`aria-checked` but no `tabIndex` management and no arrow-key handling, so all five ratings are independent tab stops and arrows do nothing (operable via Tab + Space, hence a behaviour mismatch rather than an access blocker). Replaces them with native `input type="radio"` sharing one `name`, so the browser supplies one tab stop, arrow movement with selection-follows-focus, wrapping, and direction-correct arrows under RTL — applying Story 129's recorded native-radio-over-primitive decision rather than making a new one. The input is `sr-only` and the existing visible box is styled from it through `peer`, preserving the appearance and restating `.focus-ring`'s four declarations as `peer-focus-visible:` variants over the same tokens. No dependency added (`@radix-ui/react-radio-group` is absent from the lockfile), no shared `RadioGroup` primitive, no i18n change, no backend. Planning probed the repo's own jsdom/user-event environment: tab-stop, arrow, wrap, Space, click and `sr-only` focusability all behave correctly, but **RTL arrow mirroring is not implemented there**, so the plan forbids an RTL arrow assertion and meets that criterion structurally instead. | 167 |
| redesign-the-login-screens-onto-the-shared-design-system | [redesign-the-login-screens-onto-the-shared-design-system/00-overview.md](./redesign-the-login-screens-onto-the-shared-design-system/00-overview.md) | Redesign the Login screens onto the shared design system — the first screen of the CRM UI/UX redesign initiative. Both `(auth)/login/page.tsx` become a composed entry point (identity line from the existing `common.appName`, `text-title` heading, raised `Card` with an `aria-hidden` accent rail, `FormField` fields, `Button size="lg" isLoading`, pre-auth locale switcher reusing Story 119's `languageSwitcher` keys), and the portal's hand-rolled session-expired `<p>` becomes the same `Alert` web already uses. First adoption of Story 134's named type scale, which planning re-measured at zero consumers. No token, primitive, component, dependency or translation key added; no auth request, cookie or redirect changed. Two limitations documented rather than fixed: `Button`'s accessible name under `isLoading`, and the portal overlay listener's abandoned history-patch mechanism (which is why no portal `useNavigatingRouter` is introduced). | 168 |
| _(unplanned)_ button-loading-accessible-name | Story 169 — "preserve the pending button's accessible name": `Button`'s `isLoading` hid its label with `invisible` (`visibility: hidden`), which the accessible-name computation excludes — so all 13 `isLoading` call sites announced as "button, busy" with **no name** for the duration of their own pending state. One class changes to `opacity-0`, which is visually identical and is not an exclusion criterion. Surfaced with evidence by Story 168, which had to carry a local `aria-label` workaround on both login submits; that workaround is removed here, since the primitive now owns this. Selected under CLAUDE.md §2 priority 1 (dependency correctness): every screen the UI/UX redesign touches next adopts `Button isLoading`, and each would otherwise need its own copy of the workaround. Guarded by a class-level assertion in `button.spec.tsx` — jsdom loads no Tailwind CSS, so an accessible-name assertion passes against the bug itself. No formal plan artifact (self-evidently small, §3.4). | 169 |
| type-scale-adoption | [type-scale-adoption/00-overview.md](./type-scale-adoption/00-overview.md) | Story 170 — "spend the named type scale on page and section headings." Completes the job `tailwind-preset.js` names verbatim ("…which is why pages read flat… applying them is a shared `PageHeader`'s job (NAV-2)"): `PageHeader`'s `h1` moves `text-lg font-semibold` -> `text-title` (1.5rem), `CardTitle` moves `text-sm font-semibold` -> `text-subhead` (1rem), and the three hand-rolled inline-editable detail titles follow in both their read and edit halves so the heading does not resize on Edit. `font-semibold` is dropped throughout because both steps declare `fontWeight: 600` in their own `fontSize` tuple. 37 `PageHeader` consumers and 29 `SectionCard`/`CardTitle` files inherit it with no call-site edit. **Implementation uncovered that the scale was unusable inside `@crm/ui` at all**: `cn`'s stock tailwind-merge config routes any unknown `text-*` into its text-colour group, so `cn("text-subhead text-ink")` emitted `text-ink` alone — silently, with no error. Fixed with `extendTailwindMerge` registering the seven steps under `font-size`, guarded by a new `cn.spec.ts` asserting the behaviour per step in both orders. | 170 |
| portal-navigation-overlay-detection | [portal-navigation-overlay-detection/00-overview.md](./portal-navigation-overlay-detection/00-overview.md) | Story 171 — "rebuild the portal's navigation-overlay detection on the mechanism that works." The portal still detected navigation start by patching `window.history.pushState`/`replaceState`, the mechanism `apps/web` abandoned after measuring in a real browser that Next's App Router installs a competing wrapper on the same two methods and wins — so the portal's overlay never appeared for any `<Link>` or `router.push()` navigation, while 19 unit tests said it did (they called `pushState` directly, which exercises the patch in isolation because jsdom has no Next runtime to compete). Story 168 found this while redesigning the login screens and deferred it as portal-wide infrastructure rather than bundling it into a Login change. The listener is now `apps/web`'s implementation byte-for-byte below its doc comment (document click listener gated on `defaultPrevented`, an exported `notifyNavigationStart`, the `popstate` event — no global patch), a portal `use-navigating-router.ts` was added, and all eight navigating files swapped their import; the portal has no `url-filters.ts`, so unlike web there is no exclusion. Both specs ported from web. No `apps/web`, `@crm/ui` or call-site behaviour change. | 171 |
| _(unplanned)_ cn-scale-registration | Story 172 — "register the remaining named scales with tailwind-merge." Completes what Story 170 started after probing the other three scales and measuring a second, distinct failure mode: an unknown `text-*` was **dropped** (read as a colour), but an unknown `p-*`/`rounded-*`/`shadow-*` matches no group at all, so nothing conflicts and **both** classes survive — `cn("p-4", "p-surface")` emitted both, and `cn("p-surface", "p-shell")` emitted both, so two steps of the same scale did not override each other and a caller's `className` could not replace a component's own padding or radius; which rule won fell to stylesheet source order. Registers `spacing`, `radius` and `shadow` and moves Story 170's `classGroups["'font-size']` override to the same idiomatic `theme` key, so one entry teaches the scale and every built-in group that reads it (`p`/`px`/`py`/`m`/`gap`/`space`/`inset`/…) picks it up at once. `cn.spec.ts` grew to cover every step of all four scales in both orders, asserting behaviour rather than the registered lists. No formal plan artifact (self-evidently small, §3.4); no component, token or preset changed. | 172 |
| make-the-agent-workspace-header-safe-below-sm | [make-the-agent-workspace-header-safe-below-sm/00-overview.md](./make-the-agent-workspace-header-safe-below-sm/00-overview.md) | Story 173 — "make the agent workspace header safe below `sm`." The authenticated web shell overflows horizontally at 320px (EN 354, AR 367, vs a 320 viewport), constant across routes, found by the real-browser verification pass after Stories 168–172 and invisible to every existing test because jsdom loads no Tailwind CSS. The header is a single non-wrapping flex row: no `flex-wrap`, no `min-w-0` on the controls cluster, and an unbounded `h-8 w-auto` brand image that claims 57px. Completes RM-10/RM-11, which made the navigation responsive (`hidden … sm:flex` on both nav surfaces plus the header's `sm:hidden` hamburger) and never returned for the identity/controls row. Follows the portal header's own `flex flex-wrap … gap-y-2` precedent, with `gap-y-inline` spending Story 134's `--space-inline` in place of the portal's raw `gap-y-2`. **Planning corrected the intake:** its three prescribed changes were measured to leave EN at 360 / AR at 372 once a branch switcher is present, so a fourth — `flex-wrap` on the controls cluster — is what actually satisfies that acceptance criterion. Desktop/tablet geometry verified byte-identical at 834 and 1440 in both locales. Two files, no token, primitive, dependency or Playwright infrastructure added. | 173 |
